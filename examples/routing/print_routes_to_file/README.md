This directory contains an example which emulates point to point networks that connect two hosts `h1` and `h2` via a router `r1` and prints route to a file.

## 1. print-routes-point-to-point-2.py

This program emulates point to point networks that connect two hosts `h1` and `h2` via a router `r1`. Five ping packets are sent from `h1` to `h2`, and the success/failure of these packets is reported.

<!-- The below snippet will render example code in docs website -->
<!-- #DOCS_INCLUDE: print-routes-point-to-point-2.py -->